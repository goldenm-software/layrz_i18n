name: layrz_i18n
description: "Lightweight internationalization engine for Flutter — translations, plurals, rich text and locale detection. No Material dependency."
version: 1.0.1
homepage: https://github.com/goldenm-software/layrz_i18n
repository: https://github.com/goldenm-software/layrz_i18n

platforms:
  android:
  ios:
  linux:
  macos:
  windows:
  web:

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  collection: ^1.19.0
  flutter: { sdk: flutter }
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  web: ^1.1.0

dev_dependencies:
  build_runner: ^2.10.4
  flutter_lints: ^6.0.0
  flutter_test: { sdk: flutter }
  freezed: ^3.2.5
  json_serializable: ^6.11.0

flutter:
